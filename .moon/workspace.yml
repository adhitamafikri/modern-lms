# https://moonrepo.dev/docs/config/workspace
$schema: './cache/schemas/workspace.json'

vcs:  
  manager: 'git'
  defaultBranch: 'main'

projects:
  backend: './app/backend'
  platform: './app/platform'
  admin: './app/admin'
